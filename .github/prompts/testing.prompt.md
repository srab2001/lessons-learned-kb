---
description: "Run all tests, report results, and iteratively fix failures until the suite passes."
agent: 'agent'
tools:
  - 'codebase'
  - 'terminal'
  - 'editFiles'
---

# Test Runner

Use the `test-runner` skill to run all tests, diagnose failures, and iteratively fix code or tests until the full suite passes.

## What to Do

1. Read the `test-runner` skill file
2. Follow the workflow defined in the Steps section
